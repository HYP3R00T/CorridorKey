# Model Transformer

Neural network architecture for the CorridorKey chroma keying model.

This module defines `GreenFormer`, a hybrid transformer and CNN model that predicts
an alpha matte and foreground color from an RGB image and a rough input mask.

::: corridorkey_core.model_transformer
