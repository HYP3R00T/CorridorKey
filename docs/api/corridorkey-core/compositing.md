# Compositing

Image compositing utilities used by CorridorKey.

This module contains functions responsible for combining the predicted alpha matte
with the foreground and background images to produce the final composited frame.

::: corridorkey_core.compositing
